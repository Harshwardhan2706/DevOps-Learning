![Uploading image.png…]()

[Uploading Gemini_Generated_Image_2esyhs2esyhs2esy.png…]()

🔴 Installing Homebrew on the MaC ---

  harshwardhan_baghel@Harshwardhans-MacBook-Air ~ % history
    1  brew update
    2  ls /opt/homebrew/bin/brew || ls /usr/local/bin/brew
    3  /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
    4  brew --version
    5  echo >> /Users/harshwardhan_baghel/.zprofile
    6  echo 'eval "$(/opt/homebrew/bin/brew shellenv zsh)"' >> /Users/harshwardhan_baghel/.zprofile
    7  eval "$(/opt/homebrew/bin/brew shellenv zsh)"
    8  brew help
    9  brew --version

🔴 Important Brew commands ----

    brew search [package]: Search for a tool. (e.g., brew search python)
    brew install [package]: The heavy lifter. Downloads and installs the tool.
    brew info [package]: Shows you details about a tool (its version, website, and requirements) before you install it.

    brew update: Updates Homebrew itself and the list of available software (the "map").
    brew upgrade: Actually updates the software you’ve installed to the latest versions.

    brew list: Shows everything you have installed via Brew.
    brew uninstall [package]: Removes a tool you no longer need.
    brew cleanup: Deletes old versions and "leftover" files from previous installs. It’s like taking out the trash.

    brew doctor: This scans your system for common problems and tells you exactly how to fix them. It is the first thing you should do if something goes wrong.

    brew install --cask vlc: Installs the VLC Media Player.
    brew install --cask visual-studio-code: Installs VS Code.

🔴 Install Minicube/Kubernetes ----

    Homebrew (the installer)
    OrbStack (the engine)
    Minikube (the cluster manager)
    Kubectl (the remote control)

    orb start: to start the orbstack engine

    brew install minicube kubectl: it will install minicube on system
    brew install --cask orbstack: installs OrbStack, which is a fast, light, and modern replacement for Docker Desktop.
      What does it actually install?
        When you run that command, Homebrew downloads the OrbStack application and sets up the following:
          The OrbStack GUI: A dashboard where you can see your running containers, Kubernetes nodes, and Linux machines.
          Docker CLI Tools: It installs (or links) the standard docker, docker-compose, and buildx commands so you can use them in your terminal just like you would with Docker Desktop.
          A Virtualization Engine: It sets up a highly optimized Linux kernel that runs natively on macOS's virtualization framework.
          Local Domain Names: It automatically gives your containers local URLs (like my-app.orb.local), so you don't have to mess with IP addresses.
    brew install docker: install docker first on top od OrbStack
    Then start Docker from OrbStack application
    docker ps: to see what's riunning on docker
    minikube start --driver=docker: telling Minikube to build a miniature using Docker, one-node data center inside your Mac.

🔴 Work on Minicube ----
    kubectl get nodes: current state and metadata of all registered Node objects in the cluster.
    minikube status: to check minikube running status.
    minikube start: to start minikube cluster manager.

🔴 Kubectl Commands ----
    kubectl get pods: check the pods inside the node
    kubectl get services: check the services 
    kubectl create -h: It will give all available create commands
      clusterrole           Create a cluster role
      clusterrolebinding    Create a cluster role binding for a particular cluster role
      configmap             Create a config map from a local file, directory or literal value
      cronjob               Create a cron job with the specified name
      deployment            Create a deployment with the specified name
      ingress               Create an ingress with the specified name
      job                   Create a job with the specified name
      namespace             Create a namespace with the specified name
      poddisruptionbudget   Create a pod disruption budget with the specified name
      priorityclass         Create a priority class with the specified name
      quota                 Create a quota with the specified name
      role                  Create a role with single rule
      rolebinding           Create a role binding for a particular role or cluster role
      secret                Create a secret using a specified subcommand
      service               Create a service using a specified subcommand
      serviceaccount        Create a service account with the specified name
      token                 Request a service account token
    kubectl create deployment NAME --image=image: to create deplyment using certain docker image [nginx-depl --image=nginx].
    kubectl get deployment: check the deployment
    ImagePullBackOff:  is the system's way of saying: "I tried to download the software for your Pod, but I failed, and I’m tired of trying for a moment."
    minikube start/stop: to start and stop the minikube.
    kubectl get replicaset: 
    kubectl edit deployment nginx-depl: let us edit the deployment
    kubectl logs podName: get the logs of pod to debug
    kubectl describe podName: get the deatiled execution flow and status
    kubectl exec -it PodName -- bin/bash: It allows you to open an interactive terminal session inside the running Pod.
    exit: to exit from terminal.
    kubectl delete deployment mongo-depl: delete the deployment.
    kubectl apply -f config-file.yaml: It will apply the changes to configuration file.

    kubectl get namespaces: get the existing namespaces
      Namespace	                       Purpose
      default	              Where resources go if no namespace is specified
      kube-system	          Kubernetes system components
      kube-public	          Publicly readable data
      kube-node-lease	      Node heartbeat information
    kubectl create namespace dev: create namespace for dev environment
    kubectl api-resources --namespaced=false: When you run you are listing Cluster-scoped resources only.
    kubectl api-resources --namespaced=false: When you run you are listing resources bound to namespace.
    kubectl get configmap -n <namespace>: To get the configmap created inside certain namespace.
    kubectl apply -f <configMapFile> --namespace=my-namespace: It will create configMap in my-namespace.
    Changing Active Namespace: Kubectx needs to be installed: <brew install kubectx>
    kubens: to get the list of availabe namespaces
    kubens <namespaceName>: To change The namespace
    kubectl get ns: lists all namespaces currently created in the cluster.
    kubectl get all -n kubernetes-dashboard: get all the resources under kubernetes-dashboard
    
    

    kubectl describe service nginx-service: get the deatils of the service
    kubectl get pod -o wide: is the "expert view" for inspecting pods.
    kubectl get deployment nginx-deployment -o yaml
    kubectl get deployment nginx-deployment -o yaml: retrieve the full, "source of truth" document from the etcd database and display it in its raw YAML data structure.
    kubectl get deployment nginx-deployment -o yaml > nginx-deployment-result.yaml: you are instructing your shell to take the Standard Output (stdout) of the kubectl query—which 
                                                                                    contains the full internal representation of your deployment in the cluster—and redirect it 
                                                                                    into a new physical file on your Mac named nginx-deployment-result.yaml.
    head -n 20 nginx-deployment-result.yaml: To verify the file was created and see the first few lines.
    kubectl delete -f <deployment/service config.file>: to delete those files.
    kubectl get all: to get the all component inside cluster/minikube
    echo -n 'username' | base64: binary-to-text encoding (-n: This flag tells echo not to append a trailing newline (\n))
    kubectl get all | grep mongob: get all mongodb ones
    minikube service mongo-express-service: this will assign an external IpAddress.
    kubectl logs -l app=mongodb: selects the pod by its label and show the logs.
    kubectl get svc mongo-express-service: an inspection command used to view the networking details of a specific Service.
    kubectl port-forward svc/mongo-express-service 8081:8081\n: a networking tunnel that creates a direct link between your local machine (MacBook) and a Service inside your Kubernetes cluster.
                                                                Technically, it allows you to access a private cluster resource without setting up a complex LoadBalancer or Ingress.
    kubectl rollout restart deployment mongo-express: the "cleanest" way to reboot your application without changing your configuration or deleting the resource entirely.

🔴 Ingress ====================================================================================
  🚩“Ingress does not replace Kubernetes Services. It operates at the HTTP layer and routes external traffic to Services, 
    which in turn load-balance traffic to Pods. Ingress often replaces the need for multiple NodePort or LoadBalancer services, 
    but Services are still mandatory.”

  🚩 An Ingress Controller is a Kubernetes component that actually implements Ingress rules by running 
     a reverse proxy / load balancer (like NGINX, HAProxy, Envoy).
      Ingress = rules
      Ingress Controller = engine that enforces those rules
      🔧 What the Ingress Controller Actually Does --
          Terminates TLS (HTTPS)
          Path-based routing (/api, /app)
          Host-based routing (app.example.com)
          Load balancing
          Health checks
          Rewrites, redirects, rate limits (controller-specific)

    minikube addons enable ingress: to enable ingress controller in cluster.
    kubectl get deployment -A | grep ingress: give all the pods filter by ingress
    minikube addons list: get the list of all addons
    minikube addons enable <addon> : To enable the addons
    kubectl get ingress -n kubernetes-dashboard: 
    kubectl get ingress -n kubernetes-dashboard --watch: get into live mode
    kubectl describe ingress dashboard-ingress -n kubernetes-dashboard: you are asking Kubernetes for a detailed 
                                                                        "status report" of your Ingress resource

    sudo vim /etc/hosts: opening your Mac's Local DNS Resolver file with administrative privileges.
      "By editing this file, you tell your Mac: "Before you go to the internet, check this list first. 
      If you see 'dashboard.com', send the traffic to my Minikube IP instead."

    🧲 Real-time Scenario ------ 
        1️⃣ Issue
        Kubernetes Dashboard not accessible via Ingress and custom domain on Minikube.
        
        2️⃣ Symptoms
        dashboard.com opens blank page or fails to load
        Dashboard not visible despite Ingress controller and DNS mapping
        Service remains inaccessible externally
        
        3️⃣ Root Cause
        Kubernetes Dashboard is not exposed via Ingress by default. On Minikube, Ingress has no real external IP and the dashboard requires authenticated, secure access.
        
        4️⃣ Troubleshooting
        Verified Ingress controller was running
        Confirmed DNS resolution was correct
        Checked dashboard service type (ClusterIP)
        Identified absence of Ingress resource for dashboard
        Observed authentication requirement for dashboard access
        
        5️⃣ Resolution
        Used minikube dashboard, which:
        Starts a local secure proxy on the host machine
        Forwards traffic directly to the dashboard’s ClusterIP service
        Uses current kubeconfig for authentication
        Manages TLS internally
        Opens the dashboard in the browser without external exposure  



  🧲 Real-time Scenario ------ 
      1. Issue
        Ingress controller pod not visible in kube-system namespace after installation.

      2. Symptoms
        kubectl get pods -n kube-system did not show ingress controller pod
        kubectl get pods -A | grep ingress showed ingress controller running

      3. Root Cause
        Ingress controller is not a core Kubernetes component and was installed by Minikube in the ingress-nginx namespace, not kube-system.

      4. Troubleshooting Steps
        Listed pods across all namespaces using <kubectl get pods -A>
        Identified ingress controller running in ingress-nginx namespace
        Verified by running <kubectl get pods -n ingress-nginx>

      5. Resolution
        Confirmed ingress controller was correctly deployed in ingress-nginx; no action required.

🔴 Real-time Scenario =========================================================================
  🐞 Issue ----------------------------
    mongo-express UI returns “Unauthorized” after exposing service via Minikube.
  
  📌 Symptoms --
    mongo-express pod was in Running state
    Service was created as NodePort
    Running:
      <minikube service mongo-express-service>
    opened:
      <http://127.0.0.1:<random-port>>
    Browser consistently returned:
      "Unauthorized"
  
  🔍 Root Cause ----------------------
    When using Minikube with Docker/OrbStack driver on macOS, NodePort services are exposed via a local tunnel, which maps the service to a random localhost port.
    The redirection from NodePort → 127.0.0.1:<random-port> was expected behavior, not an error.
    The actual issue was application-level:
    mongo-express enables Basic Authentication by default
    Only MongoDB connection credentials were configured using:
      ME_CONFIG_MONGODB_ADMINUSERNAME
      ME_CONFIG_MONGODB_ADMINPASSWORD
    No UI authentication credentials were configured
    Basic Auth was not explicitly disabled
    As a result, mongo-express rejected all requests with 401 Unauthorized
  
  🛠️ Troubleshooting Steps Taken ----------------------------
    1️⃣ Verified Pod and Service Health
      kubectl get pods
      kubectl get svc mongo-express-service
      Pod was running
      Service existed and correctly mapped
    
    2️⃣ Identified macOS + Minikube NodePort Behavior
      Observed that Minikube exposed the service on:
      http://127.0.0.1:<random-port>
      Confirmed this was expected due to Docker/OrbStack driver
    
    3️⃣ Eliminated Networking as Root Cause
      Used port forwarding to bypass Service and NodePort:
      kubectl port-forward svc/mongo-express-service 8081:8081
      Issue still persisted → confirmed not a Kubernetes networking problem
    
    4️⃣ Inspected Application Configuration
      kubectl describe pod <mongo-express-pod-name>
      Verified environment variables
      Found only MongoDB credentials configured
      No ME_CONFIG_BASICAUTH settings present
    
    5️⃣ Applied Fix (Disable UI Authentication)
      Edited deployment:
      kubectl edit deployment mongo-express
      Added:
        - name: ME_CONFIG_BASICAUTH
          value: "false"
    
    6️⃣ Restarted Application Pods
      kubectl rollout restart deployment mongo-express
    
    7️⃣ Re-tested Access
      kubectl port-forward svc/mongo-express-service 8081:8081
      Accessed:
        http://localhost:8081
        mongo-express UI loaded successfully without Unauthorized
  
    ✅ Final Resolution ----------------------
      The issue was caused by default Basic Authentication enabled in mongo-express
      Explicitly disabling Basic Auth and restarting the pod resolved the issue
      Kubernetes networking and service exposure were functioning correctly


🔴 Helm ====================================================
  Helm is the package manager for Kubernetes.
  Helm helps you define, install, upgrade, and manage Kubernetes applications using reusable templates called Charts.

  🚩 Core Helm Concepts ------
    1️⃣ Chart
      A Helm Chart is a packaged Kubernetes application.
      A chart contains:
        mychart/
          Chart.yaml        # Metadata
          values.yaml       # Default values
          templates/        # Kubernetes YAML templates
          charts/           # Dependencies
            
    2️⃣ Release
      When you install a chart in a cluster:
        <<< helm install my-app ./mychart >>>

      You create a Release.
        A release = running instance of a chart
        One chart → multiple releases possible
        Each release has revision history

    3️⃣ Values.yaml
      This file contains configurable parameters.
      Example:
        replicaCount: 2
        image:
          repository: nginx
          tag: latest
        
      You override values:
        <<< helm install my-app ./mychart -f prod-values.yaml >>>

    4️⃣ Templates
      Helm uses Go templating engine.
      Example inside deployment.yaml:
        <<< replicas: {{ .Values.replicaCount }} >>>

      Helm dynamically replaces values during install.

    5️⃣ Repository
      A place where charts are stored.
      Example:
        helm repo add bitnami https://charts.bitnami.com/bitnami
    
      Popular repositories:
        Bitnami
        ArtifactHub

  🚩 Helm Workflow (Step-by-Step) --------
    1. Add repository
    <<< helm repo add bitnami https://charts.bitnami.com/bitnami >>>
    
    2. Search chart
    <<< helm search repo nginx >>>
    
    3. Install chart
    <<< helm install my-nginx bitnami/nginx >>>
    
    4. List releases
    <<< helm list >>>
    
    5. Upgrade
    <<< helm upgrade my-nginx bitnami/nginx >>>
    
    6. Rollback
    <<< helm rollback my-nginx 1 >>>
    
    7. Uninstall
    <<< helm uninstall my-nginx >>>


  🚩 Helm v2 vs Helm v3
      Feature	                  Helm v2	              Helm v3
      Tiller (server component)	Required	            ❌ Removed
      Security	                RBAC issues	          More secure
      Release storage	          ConfigMaps	          Secrets
      Architecture	            Client-Server	        Client-only

  🚀 Helm in Real DevOps / SRE World
    Since you're preparing for DevOps/SRE roles, this is how Helm is used in production:

    🔹 With CI/CD
      Build image
      Update image tag in values.yaml
      helm upgrade --install
      Automated rollout
    
    🔹 With GitOps (ArgoCD / Flux)
      Charts stored in Git
      ArgoCD syncs automatically
    
    🔹 Blue-Green / Canary
      Use different releases
      Adjust replica weights

🔴 Storage Class ==================================================
  a StorageClass defines how storage should be dynamically provisioned.
  🎯 Why StorageClass Is Needed
    Without StorageClass:
      Admin must manually create PVs
      Developers depend on infra team
      Scaling becomes difficult
    
    With StorageClass:
      PVC triggers automatic volume creation
      No manual PV creation needed
      Works seamlessly in cloud environments

  🏗️ Storage Architecture Flow
    Pod → PVC → StorageClass → Provisioner → PV → Cloud Disk

  Flow Explained:
    Developer creates PVC
    PVC references a StorageClass
    Kubernetes calls provisioner
    PV is dynamically created
    Pod mounts the PVC

  🆚 StorageClass vs PersistentVolume
  Feature	 -               StorageClass	                        PersistentVolume
  Purpose	 -               Defines how storage is created	      Represents actual storage
  Dynamic provisioning -	 Yes	                                No
  Cloud integration	  -    Yes	                                Manual


🔴 Stateful vs Stateless Applications in Kubernetes =======================================

  1️⃣ Stateless Applications --------
  📌 Definition
    A stateless application does NOT store client/session data locally.
    Each request is independent.
    
    If the pod dies → nothing is lost.

  🧠 Characteristics
    No local storage dependency
    Easily scalable (horizontal scaling)
    Any replica can handle any request
    No fixed identity
    No need for Persistent Volumes

  💡 Examples
    Nginx (web server)
    Apache HTTP Server
    Frontend React apps
    API gateways
    Microservices

  📦 Kubernetes Deployment Used
    Stateless apps typically use:
    kind: Deployment

  2️⃣ Stateful Applications --------
  📌 Definition
    A stateful application stores data locally or maintains session/state information.

    If the pod dies → data must NOT be lost.

  🧠 Characteristics
    Requires persistent storage
    Each pod has unique identity
    Ordered startup & shutdown
    Stable network identity
    Uses Persistent Volume

  💡 Examples
    MySQL
    PostgreSQL
    MongoDB
    Redis
    Kafka
    Elasticsearch

  📦 Kubernetes Resource Used
    Stateful apps use:
    kind: StatefulSet









        
